def func():
    return 1, 2, 3, 4, 5

one, two, three, four, five = func()

print(one, two, three, four, five)

# returning multiply values at a time in python
# it is possible because in python comma separated value
# are treated as  tuple and function mainly return in tuple