def fun(x):
    if x == 0:
        return 100
    else:
        return "gfg"

# same function is returning different datatype value
# it is possible becz in python type is stored with value not variable

print(fun(0))

print(fun(1))